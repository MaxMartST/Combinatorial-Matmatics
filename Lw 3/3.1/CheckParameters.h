#pragma once
#include <string>
#include <algorithm>
#include <cctype>
#include <vector>

void CheckNumberVertex(std::vector<std::string> &numbel);
void CheckCoordinate(std::vector<std::string> &coordinate);